module Palette exposing (color)

import Element exposing (..)


color =
    { extraLink = rgb (215.0 / 255.0) (25.0 / 255.0) (28.0 / 255.0)
    , link = rgb (43.0 / 255.0) (131.0 / 255.0) (186.0 / 255.0)
    }
